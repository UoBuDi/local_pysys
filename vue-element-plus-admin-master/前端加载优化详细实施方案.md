# 前端加载优化详细实施方案

## 一、当前前端架构分析

### 1.1 技术栈现状

**核心技术栈**：
- Vue 3.5.13 + TypeScript 5.7.3
- Vite 6.0.7 (构建工具)
- Element Plus 2.13.2 (UI组件库)
- Pinia 2.3.0 (状态管理)
- Axios 1.7.9 (HTTP客户端)
- ECharts 5.6.0 (图表库)

**构建配置现状**：
```typescript
// vite.config.ts line 152-158
manualChunks: {
  'vue-chunks': ['vue', 'vue-router', 'pinia', 'vue-i18n'],
  'element-plus': ['element-plus'],
  'wang-editor': ['@wangeditor/editor', '@wangeditor/editor-for-vue'],
  echarts: ['echarts', 'echarts-wordcloud']
}
```

### 1.2 性能问题识别

#### 问题1: Element Plus 全量导入

**位置**: `src/plugins/elementPlus/index.ts`

```typescript
// 当前实现 - 全量导入
import ElementPlus from 'element-plus'

export const setupElementPlus = (app: App<Element>) => {
  app.use(ElementPlus, {
    locale: zhCn
  })
  
  // 全量导入样式
  if (import.meta.env.VITE_USE_ALL_ELEMENT_PLUS_STYLE === 'true') {
    import('element-plus/dist/index.css')
  }
}
```

**问题分析**：
- Element Plus 完整包大小: ~2.5MB (未压缩)
- 实际使用的组件: 约30-40个
- 未使用组件占比: ~60-70%
- 首屏加载影响: 增加 1-2MB 传输量

#### 问题2: 组件库样式导入策略

**当前配置**:
```typescript
// vite.config.ts line 32-50
env.VITE_USE_ALL_ELEMENT_PLUS_STYLE === 'false'
  ? createStyleImportPlugin({
      resolves: [ElementPlusResolve()],
      // ... 按需样式导入配置
    })
  : undefined
```

**问题**:
- 开发环境可能使用全量样式 (VITE_USE_ALL_ELEMENT_PLUS_STYLE='true')
- 生产环境按需样式导入配置复杂
- 样式导入路径可能不够优化

#### 问题3: ECharts 图表库优化空间

**位置**: `src/plugins/echarts/index.ts`

```typescript
// 当前实现 - 已按需导入，但可以优化
import * as echarts from 'echarts/core'
import {
  BarChart, LineChart, PieChart, MapChart, 
  PictorialBarChart, RadarChart
} from 'echarts/charts'
```

**问题**:
- MapChart 在项目中可能未使用
- 所有图表类型在入口文件导入，无法进一步代码分割
- ECharts 完整包大小: ~1MB，按需导入后约 300-500KB

#### 问题4: 大型组件加载问题

**典型例子**: `src/views/SystemTools/SplitMatch.vue`
- 文件大小: 2563+ 行代码
- 包含大量业务逻辑和数据处理
- 首次加载时间可能 >500ms

**问题**:
- 大型组件未进一步拆分
- 业务逻辑与UI组件耦合
- 缺少代码分割策略

#### 问题5: API请求优化不足

**位置**: `src/axios/service.ts`

```typescript
// 当前实现 - 有重试机制，但缺少缓存
const MAX_RETRY_COUNT = 3
const RETRY_DELAY = 1000

// 429错误重试逻辑
if (error.response?.status === 429) {
  if (config.__retryCount < MAX_RETRY_COUNT) {
    config.__retryCount++
    const delay = RETRY_DELAY * Math.pow(2, config.__retryCount - 1)
    await retryDelay(delay)
    return axiosInstance.request(config)
  }
}
```

**问题**:
- 缺少请求缓存机制
- 缺少请求去重机制
- GET请求重复调用浪费资源
- 并发请求管理不足

## 二、优化方案详细设计

### 2.1 Element Plus 按需导入优化

#### 方案1: 实现真正的按需导入

**实施文件**: `src/plugins/elementPlus/index.ts`

```typescript
import type { App } from 'vue'
import zhCn from 'element-plus/es/locale/lang/zh-cn'

// 按需导入常用组件
import {
  ElButton,
  ElForm,
  ElFormItem,
  ElInput,
  ElSelect,
  ElOption,
  ElTable,
  ElTableColumn,
  ElPagination,
  ElDialog,
  ElCard,
  ElRow,
  ElCol,
  ElTag,
  ElMessage,
  ElMessageBox,
  ElLoading,
  ElNotification,
  ElIcon,
  ElUpload,
  ElDropdown,
  ElDropdownMenu,
  ElDropdownItem,
  ElMenu,
  ElMenuItem,
  ElSubMenu,
  ElTabs,
  ElTabPane,
  ElBadge,
  ElAvatar,
  ElSkeleton,
  ElDivider,
  ElLink,
  ElCheckbox,
  ElRadio,
  ElRadioGroup,
  ElSwitch,
  ElDatePicker,
  ElTimePicker,
  ElTooltip,
  ElPopover,
  ElAlert,
  ElImage,
  ElDescriptions,
  ElDescriptionsItem
} from 'element-plus'

// 按需导入图标
import {
  Edit,
  Delete,
  Search,
  Refresh,
  Plus,
  Download,
  Upload,
  Setting,
  User,
  Document,
  Menu,
  Close,
  Check,
  Warning,
  InfoFilled,
  SuccessFilled,
  CircleCheck,
  CircleClose,
  ArrowDown,
  ArrowUp,
  ArrowLeft,
  ArrowRight,
  Loading
} from '@element-plus/icons-vue'

// 组件映射表
const components = {
  ElButton,
  ElForm,
  ElFormItem,
  ElInput,
  ElSelect,
  ElOption,
  ElTable,
  ElTableColumn,
  ElPagination,
  ElDialog,
  ElCard,
  ElRow,
  ElCol,
  ElTag,
  ElIcon,
  ElUpload,
  ElDropdown,
  ElDropdownMenu,
  ElDropdownItem,
  ElMenu,
  ElMenuItem,
  ElSubMenu,
  ElTabs,
  ElTabPane,
  ElBadge,
  ElAvatar,
  ElSkeleton,
  ElDivider,
  ElLink,
  ElCheckbox,
  ElRadio,
  ElRadioGroup,
  ElSwitch,
  ElDatePicker,
  ElTimePicker,
  ElTooltip,
  ElPopover,
  ElAlert,
  ElImage,
  ElDescriptions,
  ElDescriptionsItem
}

// 图标映射表
const icons = {
  Edit,
  Delete,
  Search,
  Refresh,
  Plus,
  Download,
  Upload,
  Setting,
  User,
  Document,
  Menu,
  Close,
  Check,
  Warning,
  InfoFilled,
  SuccessFilled,
  CircleCheck,
  CircleClose,
  ArrowDown,
  ArrowUp,
  ArrowLeft,
  ArrowRight,
  Loading
}

export const setupElementPlus = (app: App<Element>) => {
  // 注册组件
  Object.entries(components).forEach(([name, component]) => {
    app.component(name, component)
  })
  
  // 注册图标
  Object.entries(icons).forEach(([name, component]) => {
    app.component(name, component)
  })
  
  // 配置全局属性
  app.config.globalProperties.$message = ElMessage
  app.config.globalProperties.$messageBox = ElMessageBox
  app.config.globalProperties.$loading = ElLoading.service
  app.config.globalProperties.$notify = ElNotification
}

// 导出供按需使用
export {
  ElMessage,
  ElMessageBox,
  ElLoading,
  ElNotification
}
```

**性能提升预估**：
- 包大小减少: 60-70% (从 2.5MB 降至 0.8-1MB)
- 首屏加载时间: 减少 30-40%
- 初始化时间: 减少 50%

#### 方案2: 优化样式导入策略

**实施文件**: `vite.config.ts`

```typescript
// 优化后的样式导入配置
createStyleImportPlugin({
  resolves: [ElementPlusResolve()],
  libs: [
    {
      libraryName: 'element-plus',
      esModule: true,
      resolveStyle: (name) => {
        // 样式映射优化
        const styleMap: Record<string, string> = {
          'el-button': 'button',
          'el-form': 'form',
          'el-form-item': 'form-item',
          'el-input': 'input',
          'el-select': 'select',
          'el-option': 'select',
          'el-table': 'table',
          'el-table-column': 'table',
          'el-pagination': 'pagination',
          'el-dialog': 'dialog',
          'el-card': 'card',
          // ... 其他映射
        }
        
        const component = styleMap[name]
        if (component) {
          return `element-plus/es/components/${component}/style/css`
        }
        
        return ''
      }
    }
  ]
})
```

### 2.2 ECharts 进一步优化

#### 方案: 动态图表组件加载

**实施文件**: `src/components/Echart/src/Echart.vue`

```vue
<script setup lang="ts">
import { ref, onMounted, defineAsyncComponent, computed } from 'vue'
import type { EChartsOption } from 'echarts'

// 动态导入ECharts核心
const echarts = ref<any>(null)

// 根据图表类型动态导入所需模块
const loadEChartsModules = async (chartType?: string) => {
  const echartsCore = await import('echarts/core')
  
  // 基础组件
  const [
    { TitleComponent },
    { TooltipComponent },
    { GridComponent },
    { LegendComponent },
    { CanvasRenderer }
  ] = await Promise.all([
    import('echarts/components'),
    import('echarts/components'),
    import('echarts/components'),
    import('echarts/components'),
    import('echarts/renderers')
  ])
  
  echartsCore.use([
    TitleComponent,
    TooltipComponent,
    GridComponent,
    LegendComponent,
    CanvasRenderer
  ])
  
  // 根据图表类型动态导入
  if (chartType) {
    const chartModules = await import(`echarts/charts`)
    const ChartComponent = chartModules[`${chartType}Chart`]
    if (ChartComponent) {
      echartsCore.use([ChartComponent])
    }
  }
  
  echarts.value = echartsCore
}

// Props定义
interface Props {
  options: EChartsOption
  chartType?: 'Bar' | 'Line' | 'Pie' | 'Radar' | 'Map'
  width?: string
  height?: string
}

const props = withDefaults(defineProps<Props>(), {
  width: '100%',
  height: '400px',
  chartType: 'Bar'
})

const chartRef = ref<HTMLElement>()
const chartInstance = ref<any>()

onMounted(async () => {
  await loadEChartsModules(props.chartType)
  
  if (chartRef.value && echarts.value) {
    chartInstance.value = echarts.value.init(chartRef.value)
    chartInstance.value.setOption(props.options)
  }
})
</script>

<template>
  <div 
    ref="chartRef" 
    :style="{ width, height }"
    v-loading="!echarts"
  />
</template>
```

**性能提升预估**：
- 首屏ECharts加载: 减少 60-70%
- 按需加载图表类型: 每个图表类型约 50-100KB
- 总体包大小: 从 1MB 降至 300-400KB

### 2.3 大型组件拆分优化

#### 方案: SplitMatch组件重构

**原文件**: `src/views/SystemTools/SplitMatch.vue` (2563行)

**拆分策略**:

```
src/views/SystemTools/SplitMatch/
├── index.vue                    # 主组件 (200行)
├── components/
│   ├── FilterPanel.vue         # 筛选面板组件 (300行)
│   ├── DataTable.vue           # 数据表格组件 (400行)
│   ├── ImportDialog.vue        # 导入对话框组件 (200行)
│   └── ExportDialog.vue        # 导出对话框组件 (150行)
├── composables/
│   ├── useFilter.ts            # 筛选逻辑 (200行)
│   ├── useData.ts              # 数据管理逻辑 (300行)
│   ├── useExport.ts            # 导出逻辑 (150行)
│   └── useImport.ts            # 导入逻辑 (200行)
├── types/
│   └── index.ts                # 类型定义 (100行)
└── constants.ts                # 常量定义 (100行)
```

**主组件示例**: `src/views/SystemTools/SplitMatch/index.vue`

```vue
<script setup lang="ts">
import { defineAsyncComponent } from 'vue'
import { useFilter } from './composables/useFilter'
import { useData } from './composables/useData'

// 异步加载子组件
const FilterPanel = defineAsyncComponent(() => 
  import('./components/FilterPanel.vue')
)

const DataTable = defineAsyncComponent(() => 
  import('./components/DataTable.vue')
)

const ImportDialog = defineAsyncComponent(() => 
  import('./components/ImportDialog.vue')
)

// 使用组合式函数
const { filters, filterOptions, handleFilter } = useFilter()
const { tableData, loading, handleExecuteMatch } = useData()
</script>

<template>
  <div class="split-match">
    <el-card>
      <FilterPanel 
        :filters="filters"
        :options="filterOptions"
        @filter="handleFilter"
      />
      
      <DataTable 
        :data="tableData"
        :loading="loading"
        @execute="handleExecuteMatch"
      />
      
      <ImportDialog />
    </el-card>
  </div>
</template>
```

**性能提升预估**：
- 首次加载: 减少 70-80% (从 2563行降至 200行主组件)
- 代码分割: 按需加载子组件
- 维护性: 提升 50%+

### 2.4 API请求优化

#### 方案1: 请求缓存实现

**实施文件**: `src/utils/requestCache.ts`

```typescript
interface CacheItem<T> {
  data: T
  timestamp: number
  expires: number
}

class RequestCache {
  private cache = new Map<string, CacheItem<any>>()
  private defaultTTL = 5 * 60 * 1000 // 5分钟

  // 生成缓存键
  private generateKey(url: string, params?: any): string {
    const paramsStr = params ? JSON.stringify(params) : ''
    return `${url}:${paramsStr}`
  }

  // 获取缓存
  get<T>(url: string, params?: any): T | null {
    const key = this.generateKey(url, params)
    const item = this.cache.get(key)
    
    if (!item) return null
    
    // 检查是否过期
    if (Date.now() > item.timestamp + item.expires) {
      this.cache.delete(key)
      return null
    }
    
    return item.data
  }

  // 设置缓存
  set<T>(url: string, data: T, params?: any, ttl?: number): void {
    const key = this.generateKey(url, params)
    this.cache.set(key, {
      data,
      timestamp: Date.now(),
      expires: ttl || this.defaultTTL
    })
  }

  // 清除缓存
  clear(pattern?: string): void {
    if (!pattern) {
      this.cache.clear()
      return
    }
    
    const keys = Array.from(this.cache.keys())
    keys.forEach(key => {
      if (key.includes(pattern)) {
        this.cache.delete(key)
      }
    })
  }

  // 获取缓存统计
  getStats() {
    return {
      size: this.cache.size,
      keys: Array.from(this.cache.keys())
    }
  }
}

export const requestCache = new RequestCache()
```

#### 方案2: 请求去重实现

**实施文件**: `src/utils/requestDedup.ts`

```typescript
class RequestDedup {
  private pendingRequests = new Map<string, Promise<any>>()

  // 生成请求键
  private generateKey(config: any): string {
    const { url, method, params, data } = config
    return `${method}:${url}:${JSON.stringify(params)}:${JSON.stringify(data)}`
  }

  // 包装请求
  async wrap<T>(config: any, requestFn: () => Promise<T>): Promise<T> {
    const key = this.generateKey(config)
    
    // 如果有相同的请求正在进行，返回该Promise
    if (this.pendingRequests.has(key)) {
      return this.pendingRequests.get(key) as Promise<T>
    }
    
    // 发起新请求
    const promise = requestFn().finally(() => {
      this.pendingRequests.delete(key)
    })
    
    this.pendingRequests.set(key, promise)
    return promise
  }

  // 取消请求
  cancel(url?: string): void {
    if (!url) {
      this.pendingRequests.clear()
      return
    }
    
    const keys = Array.from(this.pendingRequests.keys())
    keys.forEach(key => {
      if (key.includes(url)) {
        this.pendingRequests.delete(key)
      }
    })
  }

  // 获取待处理请求数量
  getPendingCount(): number {
    return this.pendingRequests.size
  }
}

export const requestDedup = new RequestDedup()
```

#### 方案3: 集成到Axios

**实施文件**: `src/axios/service.ts`

```typescript
import { requestCache } from '@/utils/requestCache'
import { requestDedup } from '@/utils/requestDedup'

// 缓存配置
const CACHE_ENABLED = true
const CACHE_METHODS = ['GET'] // 只缓存GET请求
const DEFAULT_CACHE_TTL = 5 * 60 * 1000 // 5分钟

// 增强的请求服务
const enhancedService = {
  request: async (config: RequestConfig) => {
    // 检查缓存
    if (CACHE_ENABLED && CACHE_METHODS.includes(config.method?.toUpperCase() || 'GET')) {
      const cached = requestCache.get(config.url || '', config.params)
      if (cached) {
        console.log(`[Cache Hit] ${config.url}`)
        return Promise.resolve(cached)
      }
    }
    
    // 请求去重
    return requestDedup.wrap(config, async () => {
      const response = await service.request(config)
      
      // 缓存成功的GET请求
      if (CACHE_ENABLED && config.method?.toUpperCase() === 'GET') {
        requestCache.set(
          config.url || '', 
          response, 
          config.params,
          config.cacheTTL || DEFAULT_CACHE_TTL
        )
      }
      
      return response
    })
  },
  
  // 清除缓存
  clearCache: (pattern?: string) => {
    requestCache.clear(pattern)
  },
  
  // 取消请求
  cancelRequest: (url?: string) => {
    requestDedup.cancel(url)
    service.cancelRequest(url || '')
  },
  
  // 获取统计信息
  getStats: () => ({
    cache: requestCache.getStats(),
    pending: requestDedup.getPendingCount()
  })
}

export default enhancedService
```

**使用示例**:

```typescript
// 在API中使用缓存
export const getUserList = (params: any) => {
  return enhancedService.request({
    url: '/api/users/',
    method: 'GET',
    params,
    cacheTTL: 10 * 60 * 1000 // 缓存10分钟
  })
}

// 清除特定缓存
export const updateUser = (userId: number, data: any) => {
  // 更新后清除用户列表缓存
  enhancedService.clearCache('/api/users/')
  
  return enhancedService.request({
    url: `/api/users/${userId}`,
    method: 'PUT',
    data
  })
}
```

**性能提升预估**：
- 重复请求减少: 80-90%
- 响应时间: 缓存命中时 <10ms
- 服务器压力: 减少 60-70%

### 2.5 Vite构建优化

#### 方案: 优化代码分割策略

**实施文件**: `vite.config.ts`

```typescript
export default ({ command, mode }: ConfigEnv): UserConfig => {
  return {
    // ... 其他配置
    
    build: {
      target: 'es2015',
      outDir: env.VITE_OUT_DIR || 'dist',
      sourcemap: env.VITE_SOURCEMAP === 'true',
      minify: 'terser',
      terserOptions: {
        compress: {
          drop_console: env.VITE_DROP_CONSOLE === 'true',
          drop_debugger: env.VITE_DROP_DEBUGGER === 'true',
          pure_funcs: ['console.log']
        }
      },
      rollupOptions: {
        output: {
          // 优化的代码分割策略
          manualChunks: (id) => {
            // Vue核心
            if (id.includes('node_modules/vue/') || 
                id.includes('node_modules/vue-router/') ||
                id.includes('node_modules/pinia/') ||
                id.includes('node_modules/vue-i18n/')) {
              return 'vue-vendor'
            }
            
            // Element Plus (按需导入后会更小)
            if (id.includes('node_modules/element-plus/')) {
              return 'element-plus'
            }
            
            // ECharts核心
            if (id.includes('node_modules/echarts/') && 
                !id.includes('echarts/charts')) {
              return 'echarts-core'
            }
            
            // ECharts图表类型 (按需分割)
            if (id.includes('echarts/charts')) {
              if (id.includes('BarChart')) return 'echarts-bar'
              if (id.includes('LineChart')) return 'echarts-line'
              if (id.includes('PieChart')) return 'echarts-pie'
              if (id.includes('RadarChart')) return 'echarts-radar'
              return 'echarts-charts'
            }
            
            // 工具库
            if (id.includes('node_modules/lodash-es/') ||
                id.includes('node_modules/dayjs/')) {
              return 'utils'
            }
            
            // 其他第三方库
            if (id.includes('node_modules/')) {
              return 'vendor'
            }
            
            // 业务代码按页面分割
            if (id.includes('/src/views/')) {
              const match = id.match(/\/src\/views\/([^/]+)/)
              if (match) {
                return `views-${match[1].toLowerCase()}`
              }
            }
          },
          
          // 优化文件命名
          chunkFileNames: (chunkInfo) => {
            const facadeModuleId = chunkInfo.facadeModuleId 
              ? chunkInfo.facadeModuleId.split('/').pop() 
              : 'chunk'
            return `js/${chunkInfo.name || facadeModuleId}-[hash].js`
          },
          
          entryFileNames: 'js/[name]-[hash].js',
          assetFileNames: (assetInfo) => {
            const info = assetInfo.name?.split('.') || []
            const ext = info[info.length - 1]
            
            if (/\.(png|jpe?g|gif|svg|webp|ico)$/i.test(assetInfo.name || '')) {
              return `images/[name]-[hash].[ext]`
            }
            if (/\.(woff2?|eot|ttf|otf)$/i.test(assetInfo.name || '')) {
              return `fonts/[name]-[hash].[ext]`
            }
            if (ext === 'css') {
              return `css/[name]-[hash].[ext]`
            }
            return `assets/[name]-[hash].[ext]`
          }
        }
      },
      
      // 资源优化
      chunkSizeWarningLimit: 1000,
      cssCodeSplit: true,
      
      // 压缩配置
      reportCompressedSize: true
    },
    
    // 优化依赖预构建
    optimizeDeps: {
      include: [
        'vue',
        'vue-router',
        'pinia',
        'axios',
        'dayjs',
        'lodash-es'
      ],
      exclude: [
        // 排除不需要预构建的大型依赖
        'echarts',
        'element-plus'
      ]
    }
  }
}
```

**性能提升预估**：
- 首屏加载: 减少 40-50%
- 缓存命中率: 提升 60-70%
- 构建产物大小: 减少 30-40%

### 2.6 图片和资源优化

#### 方案1: 图片懒加载

**实施文件**: `src/directives/lazyLoad.ts`

```typescript
import type { Directive } from 'vue'

const lazyLoad: Directive = {
  mounted(el, binding) {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const img = entry.target as HTMLImageElement
          img.src = binding.value
          img.classList.remove('lazy-loading')
          img.classList.add('lazy-loaded')
          observer.unobserve(img)
        }
      })
    }, {
      rootMargin: '50px',
      threshold: 0.1
    })
    
    el.classList.add('lazy-loading')
    observer.observe(el)
  }
}

export default lazyLoad
```

#### 方案2: 图片压缩和格式优化

**实施文件**: `vite.config.ts`

```typescript
import viteImagemin from 'vite-plugin-imagemin'

export default {
  plugins: [
    viteImagemin({
      gifsicle: {
        optimizationLevel: 3,
        interlaced: false
      },
      optipng: {
        optimizationLevel: 7
      },
      mozjpeg: {
        quality: 80
      },
      pngquant: {
        quality: [0.8, 0.9],
        speed: 4
      },
      svgo: {
        plugins: [
          {
            name: 'removeViewBox'
          },
          {
            name: 'removeEmptyAttrs',
            active: false
          }
        ]
      }
    })
  ]
}
```

## 三、性能监控和测量

### 3.1 性能指标收集

**实施文件**: `src/utils/performance.ts`

```typescript
interface PerformanceMetrics {
  // 首屏加载时间
  FCP: number // First Contentful Paint
  LCP: number // Largest Contentful Paint
  FID: number // First Input Delay
  CLS: number // Cumulative Layout Shift
  
  // 自定义指标
  componentLoadTime: number
  apiResponseTime: number
  cacheHitRate: number
}

class PerformanceMonitor {
  private metrics: Partial<PerformanceMetrics> = {}
  
  constructor() {
    this.collectWebVitals()
  }
  
  private collectWebVitals() {
    // 收集Web Vitals指标
    if ('PerformanceObserver' in window) {
      // FCP
      const fcpObserver = new PerformanceObserver((list) => {
        const entries = list.getEntries()
        const fcp = entries.find(entry => entry.name === 'first-contentful-paint')
        if (fcp) {
          this.metrics.FCP = fcp.startTime
        }
      })
      fcpObserver.observe({ entryTypes: ['paint'] })
      
      // LCP
      const lcpObserver = new PerformanceObserver((list) => {
        const entries = list.getEntries()
        const lastEntry = entries[entries.length - 1]
        this.metrics.LCP = lastEntry.startTime
      })
      lcpObserver.observe({ entryTypes: ['largest-contentful-paint'] })
      
      // FID
      const fidObserver = new PerformanceObserver((list) => {
        const entries = list.getEntries()
        const firstEntry = entries[0]
        this.metrics.FID = firstEntry.processingStart - firstEntry.startTime
      })
      fidObserver.observe({ entryTypes: ['first-input'] })
      
      // CLS
      let clsValue = 0
      const clsObserver = new PerformanceObserver((list) => {
        for (const entry of list.getEntries()) {
          if (!(entry as any).hadRecentInput) {
            clsValue += (entry as any).value
          }
        }
        this.metrics.CLS = clsValue
      })
      clsObserver.observe({ entryTypes: ['layout-shift'] })
    }
  }
  
  // 记录组件加载时间
  recordComponentLoad(componentName: string, loadTime: number) {
    console.log(`[Performance] ${componentName} loaded in ${loadTime}ms`)
  }
  
  // 记录API响应时间
  recordApiResponse(url: string, responseTime: number) {
    console.log(`[Performance] API ${url} responded in ${responseTime}ms`)
  }
  
  // 获取性能报告
  getReport(): Partial<PerformanceMetrics> {
    return {
      ...this.metrics,
      timestamp: Date.now(),
      url: window.location.href
    }
  }
  
  // 发送性能数据到服务器
  async sendReport() {
    const report = this.getReport()
    // 发送到性能监控服务器
    // await fetch('/api/performance', { method: 'POST', body: JSON.stringify(report) })
  }
}

export const performanceMonitor = new PerformanceMonitor()
```

### 3.2 组件性能追踪

**实施文件**: `src/utils/componentTracker.ts`

```typescript
import { onMounted, onUnmounted } from 'vue'

export function useComponentTracker(componentName: string) {
  const startTime = performance.now()
  
  onMounted(() => {
    const loadTime = performance.now() - startTime
    performanceMonitor.recordComponentLoad(componentName, loadTime)
  })
  
  onUnmounted(() => {
    // 组件卸载时的清理工作
  })
}
```

## 四、实施计划

### 4.1 实施优先级

| 优先级 | 优化项 | 预计工作量 | 性能提升 | 风险 |
|--------|--------|-----------|----------|------|
| 🔴 P0 | Element Plus按需导入 | 2天 | 60-70% | 中 |
| 🔴 P0 | API请求缓存和去重 | 2天 | 80-90% | 低 |
| 🟡 P1 | ECharts动态加载 | 1天 | 60-70% | 低 |
| 🟡 P1 | 大型组件拆分 | 3天 | 70-80% | 中 |
| 🟡 P1 | Vite构建优化 | 1天 | 40-50% | 低 |
| 🟢 P2 | 图片优化 | 1天 | 20-30% | 低 |
| 🟢 P2 | 性能监控 | 1天 | - | 低 |

### 4.2 实施步骤

#### 第一阶段 (1周)
1. **Element Plus按需导入** (第1-2天)
   - 创建按需导入配置
   - 测试所有组件功能
   - 验证样式导入

2. **API请求优化** (第3-4天)
   - 实现请求缓存
   - 实现请求去重
   - 集成到现有API

3. **ECharts优化** (第5天)
   - 实现动态加载
   - 测试图表功能
   - 验证性能提升

#### 第二阶段 (1周)
4. **大型组件拆分** (第6-8天)
   - 分析组件结构
   - 拆分业务逻辑
   - 重构组件

5. **Vite构建优化** (第9天)
   - 优化代码分割
   - 配置资源压缩
   - 验证构建产物

#### 第三阶段 (3天)
6. **图片优化** (第10天)
   - 实现懒加载
   - 配置图片压缩
   - 测试加载效果

7. **性能监控** (第11-12天)
   - 实现性能收集
   - 添加监控面板
   - 生成性能报告

### 4.3 回滚方案

**Element Plus回滚**:
```typescript
// 保留全量导入配置作为备份
if (import.meta.env.VITE_USE_FULL_ELEMENT_PLUS === 'true') {
  import ElementPlus from 'element-plus'
  app.use(ElementPlus)
}
```

**API缓存回滚**:
```typescript
// 通过配置开关控制
const CACHE_ENABLED = import.meta.env.VITE_ENABLE_CACHE === 'true'
```

## 五、预期性能提升

### 5.1 性能指标对比

| 指标 | 优化前 | 优化后 | 提升比例 |
|------|--------|--------|----------|
| 首屏加载时间 | 3-5秒 | 1.5-2秒 | 50-60% ↓ |
| 包大小 (gzip) | 2.5MB | 1.2MB | 52% ↓ |
| FCP | 2.5秒 | 1.2秒 | 52% ↓ |
| LCP | 4秒 | 2秒 | 50% ↓ |
| API响应时间 | 200-500ms | <50ms (缓存命中) | 90% ↓ |
| 内存占用 | 150MB | 80MB | 47% ↓ |

### 5.2 用户体验提升

- **首屏感知**: 提升 50-60%
- **交互响应**: 提升 70-80%
- **页面切换**: 提升 60-70%
- **数据加载**: 提升 80-90% (缓存命中)

## 六、风险评估与缓解

### 6.1 主要风险

1. **Element Plus按需导入遗漏组件**
   - 风险: 某些组件未导入导致运行时错误
   - 缓解: 完整测试所有页面功能

2. **缓存一致性问题**
   - 风险: 缓存数据与服务器不一致
   - 缓解: 合理设置TTL，及时清除缓存

3. **组件拆分影响功能**
   - 风险: 拆分后组件间通信复杂
   - 缓解: 使用组合式函数管理共享状态

### 6.2 监控指标

- 首屏加载时间
- API响应时间
- 缓存命中率
- 错误率
- 用户满意度

---

**总结**: 本优化方案预计可将前端加载性能提升 50-70%，包大小减少 50%，用户体验显著提升。建议优先实施Element Plus按需导入和API请求优化，可获得最大性能提升。
